const wbm = require("wbm");

const authenticateWhatsApp = async (phone, msg) => {
  // try {
    
  // } catch (error) {
  //   console.error(error);
  //   throw new Error("Error authenticating WhatsApp");
  // }
};

module.exports = {
  authenticateWhatsApp
};
